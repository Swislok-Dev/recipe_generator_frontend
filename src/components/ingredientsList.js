import React from 'react'

export default class ingredientsList extends React.Component {
  state = {
    ingredients: [],
    instructions: "",
  }

  addIngredients = (e, idx) => {
    const ingredients = this.state.ingredients;
  }
}